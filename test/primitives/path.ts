import { expect } from 'chai';
import { Path } from '../../src/primitives/path';

describe('Primitive - Path tests', () => {
    it('Tests work', () => {
        expect(true).to.equal(true);
    });
});
