import { expect } from 'chai';
import { Text } from '../../src/primitives/text';

describe('Primitive - Text tests', () => {
    it('Tests work', () => {
        expect(true).to.equal(true);
    });
});
