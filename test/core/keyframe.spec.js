'use strict';

import {
    expect
} from 'chai/chai';
import {
    Keyframe
} from '../../src/core/keyframe';

describe('Keyframe tests', () => {
    describe('Constructor behavior', () => {
        let keyframe;

        beforeEach(function() {
            keyframe = new Keyframe();
        });

        it('Constructs correctly', () => {
        });
    });
});
